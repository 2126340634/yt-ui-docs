{
  "name": "yt-ui",
  "description": "docs for yt-ui description",
  "version": "1.2.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/deploy-build.js",
    "docs:build": "vuepress build docs",
    "docs:clean-dev": "vuepress dev docs --clean-cache",
    "docs:dev": "vuepress dev docs",
    "docs:update-package": "pnpm dlx vp-update",
    "extract": "node scripts/extract-components.js",
    "generate-llms": "node scripts/generate-llms.js",
    "sync": "npm run extract && npm run generate-llms"
  },
  "devDependencies": {
    "@vuepress/bundler-vite": "2.0.0-rc.20",
    "@vuepress/theme-default": "2.0.0-rc.88",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.2",
    "sass-embedded": "^1.86.0",
    "vue": "^3.5.13",
    "vuepress": "2.0.0-rc.20"
  }
}
